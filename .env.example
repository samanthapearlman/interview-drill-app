# Cloudflare Worker secrets — set via: wrangler secret put KEY_NAME
# Never put real keys here. This file documents what secrets are needed.
OPENAI_API_KEY=your_openai_api_key_here
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Frontend settings — entered once in Admin > Settings after deploy
# WORKER_URL=https://interview-drill.YOUR_ACCOUNT.workers.dev
